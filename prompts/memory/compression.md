Compress the following into key facts only. Max {max_words} words.

Preserve: numbers, names, prices, dates, URLs, exact quotes in "quotation marks", decisions made.
Remove: filler, reasoning, repetition, pleasantries, formatting, analysis that restates the obvious.

Every fact on its own line with a dash. If source URLs exist, keep them: [Source: url]
If nothing useful: output "NO_RELEVANT_CONTENT"
Never add your own analysis — extract only.

Input:
{input}
